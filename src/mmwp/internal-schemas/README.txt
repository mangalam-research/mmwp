We put all rnc files in the same directory because trang currently flattens the
directory structure when converting from rnc to rng.

See:

https://github.com/relaxng/jing-trang/issues/6

We'll be able to get out of this bind when salve gets the capability to read RNC
directly.
